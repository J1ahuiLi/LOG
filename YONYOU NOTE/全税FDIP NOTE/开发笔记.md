默认展开

```js
searchOpen={false}
```



```js
{
    title: '操作',
    dataIndex: 'd',
    key: 'd',
    width: 110,
    fixed: 'right',
    // exportHidden: true,
    render(text, record, index) {
    if (record.isSummaryRow) {
		return null;
	}
	return (
		<div className="operation-btn more_headSty">
		<a
			href="javascript:;"
			onClick={e => {
				_this.goToOrder(record, 2, e)
			}}
		>
    		详情
		</a>
{record.bpmState && record.bpmState !== 0 ? (
    ''
) : (
    <a
    href="javascript:;"
    onClick={e => {
    _this.goToOrder(record, 1, e)
}}
    >
        修改
</a>
)}
{record.bpmState && record.bpmState !== 0 ? (
    ''
) : (
    <a
    href="javascript:;"
    onClick={e => {
    _this.onClickDel(e, record)
}}
    >
        删除
</a>
)}
    </div>
)
},
},
```

