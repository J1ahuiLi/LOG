1. DRAM：周期性刷新

2. 阶码越长，范围越大；尾数越长，精度越高

3. X.509推荐使用RSA，SM2推荐使用ECC

4. 没有约定，著作权属于受委托人

5. 翻译权：一种自然语言文字转换成另一种自然语言文字

6. 数据流图依赖于多个逻辑条件取值，则最适宜采用判断图

7. 栈存放：返回地址，形参变量，局部变量

8. 全局变量存放在静态存储区

9. 编译器：先翻译后执行，效率高移植性差

   翻译器：边翻译边执行，效率低移植性好

10. 递归子程序 = 递归下降= 自上而下

11. 可剥夺方式：强行高优先级先执行

12. **页式存储管理系统计算**：

    - 2^（段号大 - 段号小 + 1） = 段数量
    - 2^（页号大 - 页号小 + 1） = 每段的页数
    - 2^（页内地址大 -页内地址小 + 1） = 页面大小

13. 磁盘计算：读取一个数据块的总时间 = 寻道时间 + 旋转延迟 （即旋转到达时间） + 传输时间
14. 三层C/S结构：把应用功能分成表示层、功能层、数据层。系统各层独立部署，可并行开发，选择各自的开发语言
15. 语句覆盖发现错误能力最差
16. 单一责任原则：仅有一个引起其变化的原因
17. 构件图：展示一组构件之间的组织和依赖，专注于系统的静态实现视图
18. VLAN属于链路层
19. Dijkstra采用贪心算法
20. 