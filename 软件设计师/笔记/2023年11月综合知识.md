1. 链表不能随机访问表中的任一元素，不一定是连续的，所以必须从头开始

2. 分数背包问题（部分背包问题）使用贪心算法

3. 统一过程模型：

   起始阶段：专注于项目的初创活动

   精化阶段：进行需求分析和架构演进

   构建阶段：关注系统的构建，产生实现模型

   移交阶段：关注于软件提交方面的工作，产生软件增量

4. SNMP一种网络设备管理协议，不属于安全协议

5. 方法重载（overload）通过静态绑定机制实现多态，是一种静态多态

   方法覆盖（override）是一种动态多态

6. 重载：同一个名字，不同参数

   多态：同一个方法，不同实现

7. 白盒测试原则如下:

   1. 程序模块中的所有独立路径至少执行一次
   2. 在所有的逻辑判断中，取“真”和取“假"的两种情况至少都能执行一次
   3. 每个循环都应在边界条件和一般条件下各执行一次
   4. 测试程序内部数据结构的有效性等

8. 对面向对象软件的测试可分为下列的4个层次进行：
   1. 算法层:测试类中定义的每个方法。
   2. 类层:测试封装在同一个类中的所有方法与属性之间的相互作用。
   3. 模板层:测试一组协同工作的类之间的相互作用。
   4. 系统层:把各个子系统组装成完整的面向对象软件系统，在组装过程中同时进行测试。

9. 带ES的是对称加密：AES、DES、3DES、RC4、RC5、SM4

10. 带SA的是对称加密：RSA、ECDSA、ECC、SM2

11. POP3服务默认端口为110

12. 折半查找为分治算法，，数据必须有序，时间复杂度为 O(log n)

13. 折半查找成功与不成功的平均查找长度不一样

    查找成功：每个元素查找次数之和/元素总数，即，5个元素的长度为 (1 + 2 x 2 + 3 x 4 ) / 5

14. 项目估算不考虑类型

15. python中，**x.append() 在x后追加括号里的内容**

16. 哈夫曼树：字符编码数 = 叶子节点数 = (总结点数 + 1) / 2

17. 浮点数加(减)法操作流程:

    1. 对阶
    2. 求尾数和(差)
    3. 结果规格化并判溢出
    4. 舍入处理
    5. 溢出判别

18. 非静态局部变量存储在栈区

19. 甘特图无法看出子任务间的衔接关系

20. 基本表：概念视图

    存储文件：内部视图

    视图：用户视图

21. 装饰模式可以给对象动态地添加一些额外的职责，而不改变该对象的结构
22. 报文摘要算法（MD5和SHA），可以用来保证数据的完整性，防止发送的报文被篡改。
23. 冒泡排序：对比相邻元素，前小后大，循环
24. 数据库概念结构设计阶段的工作步骤为：
    1. 抽象数据
    2. 设计局部视图
    3. 合并取消冲突
    4. 修改重构消除冗余